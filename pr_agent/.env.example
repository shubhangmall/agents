# OpenAI key for agent calls
OPENAI_API_KEY=your_openai_api_key_here

# Default model for triage and reporter agents
PR_AGENT_MODEL=gpt-4o-mini

# Model for fix agent (higher accuracy for patches)
PR_FIX_MODEL=gpt-4o

# Optional; gh auth login is the primary auth method
GITHUB_TOKEN=
